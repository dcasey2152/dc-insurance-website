# Internal dev / planning docs — never publish
DC_Insurance_Master_Log.md
DC_Insurance_Site_Audit_*.md
BLOG_QUEUE.md
BLOG_PLAN*.md
*-PREV-backup.pdf

# Local scripts, logs, temp/OS artifacts
*.bat
git-push-log.txt
_webp_tmp/
.fuse_hidden*
.DS_Store

# Archived logs and backups - internal only, never publish
log-archive/
DC_Insurance_Master_Log*.md
DC-Insurance-Development-Master-Log.md

# Credentials - NEVER commit. Added CP158 after the 2026-08-23 audit found a
# PAT in plaintext. If any of these ever appear in `git status`, stop and fix
# the ignore rule; do not commit "just this once".
.dc-github-pat
*.pat
*token*.txt
.env
.env.*
.netrc

# Local staging and internal working docs - never publish
STAGED_post*/
PUSH28/
blog-drafts-pending/
DC_INSURANCE_OUTSIDE_AUDIT_RECAP_*.md
DC_Insurance_*.md
*.patch
GSC Exports/

# Internal log tooling, never ships to the live site
checkpoint.sh
